<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fma.fmapos">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".facade.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".facade.LoginActivity" />
        <activity android:name=".facade.OrderCreateActivity" />
        <activity android:name=".facade.ProductCreateActivity" />
        <activity android:name=".facade.CustomerCreateActivity" />
        <activity android:name=".facade.PresetCreateActivity" />
        <activity android:name=".facade.PaymentActivity" />
        <activity
            android:name=".facade.OrderFinishActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".facade.OrderActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".facade.OrderHistoryActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".facade.ProductActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".facade.CustomerActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".facade.SettingActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".facade.PresetActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".facade.RestActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".facade.SelectUnitActivity"></activity>
    </application>

</manifest>